def verify(username, password, dob):
    """Perform SQL verification here

    Args:
        username (string): Name of the Person in FirstName<Space>Lastname
        password (string): Password of the User
        dob (string): Date of Birth as YYYY-MM-DD

    Returns:
        Boolean: Whether the check succeeded or not
    """
    return True
